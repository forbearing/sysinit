	1:打开窗口
		split
		vsplit
		split file
		new/vnew
		{height} split file
	2:跳转窗口:
		<C-W>h
		<C-W>j
		<C-W>k
		<C-W>l
		<C-W>t		最顶上
		<C-W>b		最底下
	3:改变窗口大小
		<C-W>-
		{height}<C-W>-		
		<C-W>+
		{height}<C-W>+
		<C-W>__					# 将窗口扩展到尽可能大
		{height}<C-W>_			# 设置为指定高度
	4:移动窗口:
		<C-W>H
		<C-W>J
		<C-W>K
		<C-W>L
	5:关闭窗口
		<C-W>q
		<C-W>c
		<C-W>o
		:wall
		:qll
		:wqall
	6:在vim内部进行文件比较:
		diffsplit file
		vertical difffsplit file
		:set scrollbind			# 同步滚动两个文件
		]c						# 跳到下一个修改点
		[c						# 跳到上一个修改点
		{count}]c
		diffupdate				# 消除差异
		diffget					# 向另一个文件靠拢
		diffput					# 另一个文件向自己靠拢
	7:
		winheight:设置最小的期望窗口高度
		winminheight:	# 设置最小的"硬性"高度
		winwidth
		winminwidth
		equalalways:	# 所有的窗口在关闭或者打开新窗口的时候总保持相同大小
		laststatus
		splitbelow		# 新分割的窗口在下面
		splitright		# 新分割的窗口在右边
	8:标签页:
		:tab split		
		:tabonly		# 关闭所有标签页,除了当前标签
