plugins=(git
    osx
    brew
    bundler
    kubectl
    helm
    docker
    docker-compose
    docker-machine
    ansible
    pip
    git
    zsh-autosuggestions
    zsh-syntax-highlighting
    zsh-completions
    zsh-osx-autoproxy
    zsh_reload                      # 提供一个 src 命令，重载 zsh
    themes                          # 提供一条 theme 命令，用来随时手动切换主题
    cp                              # 提供一个 cpv 命令，这个命令使用 rsync 实现带进度条的复制功能
    z                               # 提供一个 z 命令，在常用目录之间跳转。类似 autojump，但是不需要额外安装软件
    vim-mode                        # vim 输入模式，非常强大
    per-directory-history           # 开启之后在一个目录下只能查询到这个目录下的历史命令。按 Ctrl+g 开启/关闭
    history-substring-search        # 一般会在 zsh 中绑定 history-search-backward 与 histor-search-forward 两个功能
    command-not-found               # 当你输入一条不存在的命令时，会自动查询这条命令可以如何获得
    safe-paste                      # 粘贴别人的脚本不会立刻执行
    colored-man-pages               # 给你带颜色的 man 命令
    sudo                            # 双击 Esc，zsh 会把上一条命令加上 sudo
    extract                         # 别名 x，一键解压
    rand-quote                      # 提供一条 quote 命令，显示随机名言。和fortune的作用差不多
    git-open                        # 提供一个 git-open 命令，在浏览器中打开当前所在 git 项目的远程仓库地址
    autojump                        # This plugin loads the [autojump navigation tool](https://github.com/wting/autojump).
    copyfile                        # copyfile <filename>, Puts the contents of a file in your system clipboard so you can paste it anywhere.
    copybuffer                      # This plugin binds the ctrl-o keyboard shortcut to a command that copies the text. that is currently typed in the command line ($BUFFER) to the system clipboard.
    copydir                         # Copies the path of your current folder to the system clipboard.
    macports                        # This plugin adds completion for the package manager [Macports](https://macports.com/),
    web-search                      # This plugin adds aliases for searching with Google, Wiki, Bing, YouTube and other popular services.
    )
